import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  container: {},
  img: {
    position: 'absolute',
    top: 0,
    left: 0,
    width: 160,
    height: 85,
  },
})

export default styles
