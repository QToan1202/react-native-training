import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  form: {
    marginTop: 36,
    marginHorizontal: 27,
    rowGap: 6,
  },
  error: {
    paddingLeft: 0,
  },
})

export default styles
