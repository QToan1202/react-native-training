import { IIconList } from '@types'

const HOME: IIconList[] = [
  { label: 'love', icon: require('@assets/love.png'), action: () => undefined },
  { label: 'cart', icon: require('@assets/cart.png'), action: () => undefined },
]

export default { HOME }
